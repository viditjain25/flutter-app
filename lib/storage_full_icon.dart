import 'package:flutter/cupertino.dart';

class StorageFullIcon extends StatefulWidget {
  const StorageFullIcon({Key? key}) : super(key: key);

  @override
  State<StorageFullIcon> createState() => _StorageFullIconState();
}

class _StorageFullIconState extends State<StorageFullIcon> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
